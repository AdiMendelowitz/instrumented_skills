# Runtime output, never source
token-aware/tools/cost_log.jsonl
**/*.kbindex.json
**/*.bak-*
**/__pycache__/
**/*.pyc
.pytest_cache/

# Critique logs, retro journal, and handoff snapshots, if a surface with a persistent
# filesystem writes them inside this repo
.claude/critique-log/
.claude/retro-log/
.claude/handoff/

# Editor/IDE project files (can contain local machine paths)
.idea/
.vscode/
*.iml

# Blog drafts and review history: not part of the public portfolio
blog/

